1) To start camera : roslaunch ros_opencv uvcCameraLaunch.launch

2) To start apriltags : roslaunch apriltags apriltags.launch


3) ROS_NAMESPACE=camera rosrun image_proc image_proc

4)To see pose : rostopic echo /apriltags/detections
